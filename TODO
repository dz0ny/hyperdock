* support volumes
  - /volumes/(container_id if not shared / image_if if shared)
  - shared volume ? bool
  - test znc

###
Created a test droplet for this
We will also create the provisioning script in this process.

Provision a new host:

```
bin/rake host:provision host="198.199.112.194" password="bzsvdlaydnmr"
```

###


  
* Limit to 2 containers

* Default to 0 invites

* install statsd on prod w/ supervisor
  - use console backend
  
* use https://github.com/collectd/collectd to ship host-level data to statsd
  - https://collectd.org/wiki/index.php/Plugin:StatsD
   cpu memory disk network

* fix port binding bug on edit image

* restart button

